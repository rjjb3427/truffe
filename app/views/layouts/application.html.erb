<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <meta name="description" content="Jazz Bassist Otsuka Yoshimasa's Website">
  <meta name="keywords" content="yoshimasa otsuka bass jazz">
  <meta name="viewport" content="width=device-width, initial-scale=0.4, maximum-scale=1, user-scalable=no">
  <title><%= title %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="wrapper" class="l">
  <header>
    <%= render :partial => 'shared/nav' %>
  </header>

  <div id="container">
    <% flash.each do |key, value| %>
      <p class="flash <%= key %>"><%= value %></p>
    <% end %>
    <% if @page_title %>
      <h1 class="page_title"><%= h(@page_title) %></h1>
    <% end %>
    <%= yield %>
    <div class="page_footer"></div>
  </div>

  <footer>
    <div id="open_login">
      <% if signed_in? %>
        <%= link_to "Your Profile", current_user %>
        <%= link_to t('signout.submit'), signout_path, :method => :delte %>
      <% else %>
        <%= link_to t('signin.hidden_link'), signin_path %>
      <% end %>
    </div>
  </footer>
  <%= debug(params) if Rails.env.development? %>
</div>

</body>
</html>
